SELECT ? AS PKTABLE_CAT,
    NULL AS PKTABLE_SCHEM,
    ? AS PKTABLE_NAME,
    ? AS PKCOLUMN_NAME,
    ? AS FKTABLE_CAT,
    NULL AS FKTABLE_SCHEM,
    ? AS FKTABLE_NAME,
    ? AS FKCOLUMN_NAME,
    ? AS KEY_SEQ,
    ? AS UPDATE_RULE,
    ? AS DELETE_RULE,
    NULL AS FK_NAME,
    NULL AS PK_NAME,
    ? AS DEFERRABILITY
