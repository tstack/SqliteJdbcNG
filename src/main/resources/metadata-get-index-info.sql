SELECT ? AS TABLE_CAT,
    null AS TABLE_SCHEM,
    ? AS TABLE_NAME,
    ? AS NON_UNIQUE,
    ? AS TABLE_QUALIFIER,
    ? AS INDEX_NAME,
    ? AS TYPE,
    ? AS ORDINAL_POSITION,
    ? AS COLUMN_NAME,
    null AS ASC_OR_DESC,
    0 AS CARDINALITY,
    0 AS PAGES,
    null AS FILTER_CONDITION
